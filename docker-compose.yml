services:
  intent-engine:
    build:
      context: ./intent-engine
    ports:
      - "8001:8001"

  gateway:
    build:
      context: ./gateway
    environment:
      CHIRAG_INTENT_ENGINE_URL: http://intent-engine:8001
    depends_on:
      - intent-engine
    ports:
      - "8000:8000"
